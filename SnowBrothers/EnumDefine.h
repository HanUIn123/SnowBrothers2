#pragma once
enum LineDirection
{
	LD_LEFT,
	LD_RIGHT,
	END
};

enum class OBJECT_LIST
{
	PLAYER, MONSTER, BOSS, MINION,ITEM, BULLET, BOSSBULLET, MONSTERBULLET, TILE, MOUSE, UI, EFFECT, BUTTON, OL_END
};

enum class OBJECT_STATE
{
	OBJ_NOEVENT,
	OBJ_DEAD
};

enum class OBJECT_DIRECTION
{
	DIR_LEFT,
	DIR_RIGHT,
	DIR_UP,
	DIR_DOWN,
	DIR_LU,
	DIR_RU,
	DIR_RANDOM,
	DIR_END
};

enum class ITEM_LisT
{
	IT_SPEED,
	IT_POWER,
	IT_RANGE,
	IT_END
};

enum class MONSTER_STATE
{
	MS_IDLE,
	MS_WALK,
	MS_JUMP,
	MS_SHOOT,
	MS_DEAD,
	MS_END
};

enum class BOSS_STATE
{
	BS_IDLE,
	BS_WALK,
	BS_JUMP,
	BS_SHOOT,
	BS_DEAD,
	BS_END
};

enum class MONSTER_SNOW_STATE
{
	MS_SNOW1,
	MS_SNOW2,
	MS_SNOW3,
	MS_SNOW4,
	MS_SNOW5,
	MS_SNOW6,
	MS_END
};

enum class UI_STATE
{
	UI_NONE,
	UI_SHOW,
	UI_OFF
};

enum class UI_LIST
{
	UI_TIMER,
	UI_HPBAR,
	UI_LIFE,
	UI_GREAT,
	UI_BOUNDARY,

	UI_WORLDCLEAR,


	UI_END
};

enum CHANNELID
{
	SOUND_EFFECT,
	PLAYER_DEAD,
	BOSS_FC,
	BOSS_NOSE,
	TRAIN_NOISE,
	SOUND_ITEM,
	SOUND_SCENE,
	SOUND_UNSTOP,
	SOUND_BGM,
	MAXCHANNEL
};


enum class MONSTER_SPAWNER
{
	NONE = -1,
	MONSTERONE,
	MONSTERTWO,
	MONSTERTHREE,
	END
};
